# incentive-engine-api/.env.example

# Master API key (shared with SDK clients)
INCENTIVE_API_KEY=your_master_api_key_here

# Database connection URL (Postgres, MySQL, or SQLite for local dev)
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname

# Custody provider credentials (Circle, Fireblocks, etc.)
CIRCLE_API_KEY=your_circle_api_key_here
CIRCLE_WALLET_ID=your_circle_wallet_id_here

# (Optional) Host/port for the FastAPI server
API_HOST=0.0.0.0
API_PORT=8000
